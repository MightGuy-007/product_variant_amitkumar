<odoo>
    <record id="product_variant_form_view" model="ir.ui.view">
        <field name="name">product_variant_form_view</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales']" position="after">
                <page name="Part specification">
                    <group>
                        <group>
                            <field name="is_multi_parts"/>
                        </group>

                        <separator/>

                        <group>
                            <field name="material_id" invisible="is_multi_parts"/>

                            <field name="surface_finish_id" invisible="is_multi_parts"/>

                            <field name="uv_printing_id" invisible="is_multi_parts"/>

                            <field name="color_shade_id" invisible="is_multi_parts"/>
                        </group>

                    </group>
                    <notebook>
                        <page>
                            <field name="part_lines_ids" invisible="not is_multi_parts">
                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="qty"/>
                                    <field name="unit_cost"/>
                                    <field name="total_cost"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </page>
            </xpath>
        </field>
    </record>
</odoo>